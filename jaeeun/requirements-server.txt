# GPU 서버에서 웹 화면까지 함께 돌릴 때 설치하는 목록.
#
# VESSL의 PyTorch 이미지에는 CUDA용 torch와 torchvision이 이미 들어 있으므로 빼둔다.
# 다시 설치하면 CPU 판으로 덮여 가속이 사라질 수 있다.
#
# 이 파일은 requirements.txt(내 컴퓨터용)와 requirements-hair.txt(격리 환경용)를 한
# 인터프리터에서 함께 쓰기 위한 것이다. 서버는 이미지 하나로 만들어져 격리할 대상이 없다.

# 웹 화면과 이미지·영상 처리
streamlit>=1.37,<2
starlette>=0.46,<1.4
Pillow>=10.3,<12
opencv-python-headless>=4.10,<5
numpy>=1.26,<2

# 부위 분할과 생성 모델
transformers>=4.45,<5
diffusers>=0.30,<1
accelerate>=0.34,<2
safetensors>=0.4,<1
huggingface-hub>=0.25,<1
sentencepiece>=0.2,<1
protobuf>=5,<7

# SAM 2 실행에 필요한 것들 (sam2 자체는 Meta 저장소에서 설치)
hydra-core>=1.3,<2
iopath>=0.1.10,<1
tqdm>=4.66,<5

# HairFastGAN이 직접 가져다 쓰는 것들
scipy>=1.11,<2
scikit-image>=0.22,<1
scikit-learn>=1.3,<2
pandas>=2,<3
matplotlib>=3.7,<4
imageio>=2.34,<3
torchmetrics>=1,<2
dlib-bin>=19.24
face_alignment>=1.4,<2
pyyaml>=6,<7
addict>=2.4,<3
dill>=0.3,<1
gdown>=5,<6
requests>=2.31,<3
ftfy>=6,<7
regex>=2023.12
clip @ git+https://github.com/openai/CLIP.git
